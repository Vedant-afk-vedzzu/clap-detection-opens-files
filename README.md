# clap-detection-opens-files
Clap.py is a lightweight Python script that listens for audio input from your microphone and detects double claps. When two claps occur within a set time window, the script executes a customizable system command — for example, opening a website, launching an application, or running another script.
